Вот структурированный и профессиональный файл **README.md**, который описывает ваш проект. Он включает инструкции по установке, настройке и описание архитектуры.

---

# AI News Content Generator API

Этот сервис на базе **FastAPI** автоматически собирает актуальные новости по заданной теме и генерирует на их основе оптимизированный контент для блога: привлекательный заголовок, SEO мета-описание и структурированную статью.

## 🚀 Основные возможности

* **Асинхронность:** Использует `AsyncOpenAI` и `httpx` для высокой производительности и отсутствия блокировок.
* **Параллельная генерация:** Заголовок и основной текст генерируются одновременно через `asyncio.gather`, что сокращает время ожидания.
* **Интеграция с новостями:** Использует **Currents API** для получения самых свежих мировых событий.
* **SEO-оптимизация:** Генерирует мета-описания и структуру текста с учетом ключевых слов.

---

## 🛠 Технологический стек

* **Python 3.10+**
* **FastAPI** (Web Framework)
* **OpenAI GPT-4o-mini** (LLM)
* **HTTPX** (Async HTTP Client)
* **Uvicorn** (ASGI Server)

---

## 📋 Предварительные требования

У вас должны быть получены следующие API ключи:

1. [OpenAI API Key](https://platform.openai.com/)
2. [Currents API Key](https://currentsapi.services/)

---

## 🔧 Установка и запуск

### 1. Клонирование репозитория

```bash
git clone https://github.com/youruser/ai-content-generator.git
cd ai-content-generator

```

### 2. Настройка переменных окружения

Создайте файл `.env` в корневой папке проекта или экспортируйте переменные напрямую:

```bash
export OPENAI_API_KEY="ваш_ключ_openai"
export CURRENTS_API_KEY="ваш_ключ_currents"

```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt

```

### 4. Запуск сервера

```bash
python main.py

```

Сервис будет доступен по адресу: `http://localhost:8000`

---

## 📡 Использование API

### Генерировать пост

**Эндпоинт:** `POST /generate-post`

**Пример запроса (JSON):**

```json
{
  "topic": "Будущее искусственного интеллекта в 2026 году"
}

```

**Пример ответа:**

```json
{
  "title": "ИИ в 2026: От помощников к автономным агентам",
  "meta_description": "Узнайте о главных трендах развития ИИ в 2026 году на основе последних новостей индустрии...",
  "post_content": "### Введение...\n[Длинный структурированный текст статьи]...\n### Заключение..."
}

```

---

## 🏗 Структура проекта

* `main.py` — основной файл приложения, логика FastAPI и асинхронных вызовов.
* `requirements.txt` — список необходимых библиотек.
* `.env` — (не включен в репозиторий) файл с секретными ключами.

---

## 📄 Лицензия

Этот проект распространяется под лицензией MIT.

---

**Хотите, чтобы я добавил в README раздел с инструкцией по деплою на Docker?**
